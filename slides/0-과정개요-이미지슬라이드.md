# 과정 개요 (이미지 슬라이드 판독본)

## 1. IT 인프라의 구성 요소

IT Infrastructure Components — 다섯 축입니다.

| 구성 요소 | |
|---|---|
| Hardware | 물리 장비 |
| Software | 운영체제·애플리케이션 |
| Networks | 연결 |
| Data Centers | 설치·운영 공간 |
| Cloud Services | 클라우드 |

## 2. 정보보호관리체계(ISMS-P)와 이 과정의 범위

**정보보안의 3요소: 기밀성 / 무결성 / 가용성**

보안은 세 축으로 나뉩니다.

- **물리적 보안**
- **관리적 보안**
- **기술적 보안** ← 이 과정이 다루는 부분

기술적 보안 안에서 배우는 세 갈래입니다.

| 분야 | 내용 |
|---|---|
| **네트워크 보안** | 스위칭 / 라우팅 / 접근제어정책 등<br>VPN / NAT / IDS(IPS) / NMS<br>Firewall / DDoS / 프로젝트 등 |
| **시스템 보안** | 다양한 서버 구축 및 관리(보안) 설정<br>BoF / Log 관리 / 쉘 스크립트 / 프로젝트 등 |
| **웹 보안** | 웹 공격 탐지 및 로그 관리<br>모의해킹 / CTF / 워게임 / OWASP<br>모의해킹 포트폴리오 작성 및 발표 |

> 이 표가 커리큘럼 전체의 뼈대입니다. 「커리큘럼 지도」의 부 구분과 그대로 대응합니다.

## 3. OSI 7계층 (ISO-OSI 표준 모델)

| OSI 7 Layer | 기능 |
|---|---|
| Application (응용-7계층) | 사용자 인터페이스 제공 (사용자층의 인터넷 서비스 지원 등) |
| Presentation (표현-6계층) | 데이터의 의미와 표현 방법 (암호화, 압축 등) |
| Session (세션-5계층) | 통신 연결 관리 (토큰 제어, 동기화 등) |
| Transport (전송-4계층) | 단대단(End to End) 통신 기능 (흐름 제어 등) |
| Network (네트워크-3계층) | 라우팅 문제 해결 (최적 경로 설정, 혼잡 제어 등) |
| Data Link (데이터링크-2계층) | 물리적 전송 오류 해결 (전송 기능, 흐름 제어 등) |
| Physical (물리-1계층) | 전송 매체의 물리적 인터페이스 사항 (데이터의 전송 속도, 클록 동기화, 물리적 연결 형태 등) |

계층 묶음과 데이터 흐름:

- **Upper Layer (사용자 지원)** — 7·6·5계층
- **Transport Layer (전송)** — 4계층
- **Lower Layer (네트워크 지원)** — 3·2·1계층

보내는 쪽은 위에서 아래로 내려가며 **Encapsulation(캡슐화)**, 받는 쪽은 아래에서 위로
올라가며 **De-encapsulation(역 캡슐화)** 합니다. 물리 구간(Physical Link)에서 실제로 오갑니다.

## 4. OSI 모델과 TCP/IP 모델 대응

| OSI Model | 데이터 표현 | TCP/IP Model | Protocol |
|---|---|---|---|
| Application / Presentation / Session | Data | Application | FTP(21), TELNET(23), SMTP(25), HTTP(80), SNMP(169), ETC |
| Transport | Segment | Transport | TCP, UDP |
| Network | Packet | Internet | IP, ICMP, IGMP |
| Data Link / Physical | Frame / Bit | Network Access (Link) | Network Interface |

> 슬라이드에 SNMP 포트가 **169**로 적혀 있습니다. 공식 표준은 **161**(SNMP), **162**(SNMP Trap)입니다.
> 부록의 「원문과 공식 문서가 어긋나는 곳」을 함께 보십시오.

## 5. IP 주소 체계

출처 표기: `https://한국인터넷정보센터.한국`

| Network Class | Start Octets | Address Range(범위) | Private(사설) Address | 특수 용도 |
|---|---|---|---|---|
| A | 1~126 | 1.0.0.0~126.255.255.254 | 10.0.0.0/8 | 0.0.0.0/8 – 자체 네트워크 |
| B | 128~191 | 128.0.0.1~191.255.255.254 | 172.16.0.0/12 | 169.254.0.0/16 – 링크로컬(link local) |
| C | 192~223 | 192.0.0.1~223.255.255.254 | 192.168.0.0/16 | 192.0.2.0/24 – 예제 등 문서에서 사용 |
| D | 224~239 | 224.0.0.1~239.255.255.254 | Multicasting(멀티캐스팅) | 192.88.99.0/24 – 6to4 릴레이 에이전트 |
| E | 240~255 | 240.0.0.1~255.255.255.254 | 실험, 연구용 미래 사용 용도 | 198.18.0.0/15 – 네트워크 장비 벤치마킹 테스트 |

- **127.0.0.0/8 – 루프백(Loopback)** — A와 B 사이에 따로 표시되어 있습니다.
- **IPv4** — 32bit / 2진수 표기를 10진수로 표현
- **IPv6** — 128bit / 16진수 표기

이어서 **서브넷팅(Subnetting) 개념**으로 넘어갑니다.

## 6. ISO 통신망 관리 모델 (FCAPS)

Network Management를 다섯 축으로 나눕니다.

| 축 | 하는 일 |
|---|---|
| **Fault** (장애) | 경보 감시, 고장 위치의 측정 및 점검 |
| **Configuration** (구성) | 설비 및 설치 지원, 상태 제어 |
| **Account** (계정) | 계정(과금) 정보의 수집/저장/제어 등 |
| **Performance** (성능) | 성능 감시, 트래픽 관리, 품질관리, 통계관리 |
| **Security** (보안) | 보안, 안전, 기밀 관리 등 |

## 7. 시스코 장비 모드

| Mode | Prompt | Comment |
|---|---|---|
| User EXEC Mode (이용자 모드) | `Router>enable` | 제한적으로 이용이 가능한 모드 |
| Privileged EXEC Mode (관리자 모드) | `Router#configure terminal` | 디버깅하고 테스터하기 위한 모드 |
| Global Configuration Mode (전역 설정 모드) | `Router(config)#` | 전반적 설정이 가능한 모드 |
| Other Configuration Mode (하위 설정 모드) | `Router(config-?)#` | 환경에 따른 세부 설정이 가능한 모드 |
| Setup Mode | `Router#setup` | 초기 설정 시 사용 가능한 모드 |
| RXBoot Mode | `Router<boot>` | 이미지(IOS) 파일 문제 및 PW 모를 경우 복구하기 위한 모드 |
| ROM Monitor Mode | `>` or `rommon>` | 부팅 중 운영체제(IOS)를 로딩하지 못했을 때 수정하기 위한 모드 |
| Sensitive Help 기능 / Tab 키 활용 | `#?` / `#line ?` / `#en` + Tab | 사용 가능한 명령어 모음 도움말, 명령어 완성 기능 |

빠져나오는 방향은 `exit`(한 단계 위) 와 `end` 또는 `Ctrl+Z`(특권 모드로 한 번에)입니다.

## 8. 네트워크 실습 — 기본 설정 명령어

CISCO Packet Tracer 파일이 함께 제공되었습니다.

```
Router>enable
Router#configure terminal
Enter configuration commands, one per line.  End with CNTL/Z.
Router(config)#hostname MyRouter                     ← 장비이름 설정
MyRouter(config)#enable password 1234                ← enable 명령어 입력 시 비밀번호 적용
MyRouter(config)#enable secret 12345                 ← secret 패스워드 우선 적용
MyRouter(config)#service password-encryption         ← 설정된 모든 패스워드 암호화
MyRouter(config)#banner motd #Welcome To MyRouter#   ← 부팅 시 메시지 표시
MyRouter(config)#interface giga0/0
MyRouter(config-if)#ip address 192.168.100.254 255.255.255.0
MyRouter(config-if)#no shutdown                      ← 인터페이스에 IP주소 설정 및 활성화
MyRouter(config-if)#
%LINK-5-CHANGED: Interface GigabitEthernet0/0, changed state to up

MyRouter(config-if)#exit
MyRouter(config)#line console 0
MyRouter(config-line)#login
% Login disabled on line 0, until 'password' is set
MyRouter(config-line)#password 12345
MyRouter(config-line)#exit
MyRouter(config)#line vty 0 5                        ← 콘솔/원격접속 시 암호 설정
MyRouter(config-line)#password 12345                 ← Aux 포트로 접속 시 -> #line aux 0
MyRouter(config-line)#login
MyRouter(config-line)#
```

읽을 점 두 가지입니다.

- `enable password`와 `enable secret`을 둘 다 설정하면 **secret이 우선** 적용됩니다.
- `login`을 먼저 치면 `% Login disabled ... until 'password' is set`이 나옵니다.
  비밀번호를 정한 뒤에야 로그인이 켜집니다.

## 9. 스위치 — VLAN 설정

```
S1(config)#vlan 10
S1(config-vlan)#vlan 20
S1(config-vlan)#interface FastEthernet 0/1
S1(config-if)#switchport mode access
S1(config-if)#switchport access vlan 10
S1(config-if)#interface FastEthernet 0/2
S1(config-if)#switchport mode access
S1(config-if)#switchport access vlan 20

S1(config-if)#interface FastEthernet 0/3
S1(config-if)#switchport mode trunk
S1(config-if)#switchport trunk encapsulation dot1q
```

라우터 쪽에서 서브인터페이스로 VLAN 간 라우팅을 만듭니다 (router-on-a-stick).

```
Router(config-if)#interface FastEthernet 0/0.10
Router(config-subif)#encapsulation dot1q 10
Router(config-subif)#ip address 10.1.10.254 255.255.255.0
Router(config-subif)#exit
Router(config)#interface FastEthernet 0/0.20
Router(config-subif)#encapsulation dot1q 20
Router(config-subif)#ip address 10.1.20.254 255.255.255.0
```

## 10. 라우팅 프로토콜 비교

| Characteristic | RIPv1 | RIPv2 | IGRP | EIGRP\*\* | OSPF |
|---|---|---|---|---|---|
| Distance vector | X | X | X | X | |
| Link-state | | | | | X |
| Classful (auto route summ.) | X | X | X | X | |
| Classless (VLSM support) | | X | | X | X |
| Proprietary | | | X | X | |
| Scalability | Small | Small | Med. | Large | Large |
| Convergence time | Slow | Slow | Slow | Fast | Fast |

\*\* EIGRP is an advanced distance vector protocol

## 11. 스태틱 라우팅

**네트워크 관리자가 직접 목적지 별로 경로를 지정해 주는 것을 의미**합니다.

**장점**
- Router CPU상에 부담이 없으며, 대역폭을 낭비하는 일이 없다.
- 보안성이 있다.

**단점**
- Router가 어떻게 연결되어 있는지 정확히 알고 있어야 한다.
- 한 회선이 추가될 경우 추가된 경로를 모든 Router들한테 설정을 해주어야 하며,
  네트워크 규모가 커지면 설정 및 관리가 복잡하다.
- 네트워크 변화를 제대로 반영하지 못한다.

```
Router(config)# ip route [Destination-network] [Destination-mask]
                         [next-hop-address | exitinterface] [Administrative-distance]
```

| 인자 | 뜻 |
|---|---|
| Destination-network | 목적지 네트워크를 지정함 |
| Destination-mask | 목적지 서브넷 마스크 |
| Next-hop-address | 목적지로 갈 수 있는 경로를 지정함 (다음 Router의 Serial Interface의 IP Address) |
| Exitinterface | 목적지에 도착을 하기 위해서 사용되는 Interface |
| Administrative-distance | 거리의 값 지정 |

Dynamic Routing은 RIP / EIGRP / OSPF 등입니다.

## 12. ACL — 표준

**ACL(Access Control List)은 접속 제어 목록을 통해 패킷을 제어하기 위해 사용하는
일반적인 라우터 보안 설정**입니다.

```
Router(config)#access-list access-list-number {permit | deny} source [mask]
```

- **Access-list-number** : Entry가 속할 list 번호 설정. **1~99, 1300~1999** 사이의 번호가 들어간다
- **permit | deny | remark** 는 해당 Entry에 매치되면 취할 Action을 정의
- **Source** 는 송신지 IP Address를 정의한다
- **mask** 는 Wildcard mask를 사용하여 Address 필드의 어느 비트들이 일치되어야 하는지 설정한다

```
Router(config-if)#ip access-group access-list-number {in | out}
```

- List를 적용할 Interface에서 설정한다
- Inbound 또는 Outbound시 검사하도록 설정한다
- **Default = outbound**
- Interface에서 `no ip access-group access-list-number` 명령을 사용하여 적용된 Access-list를 제거한다

## 13. ACL — 확장

```
Router(config)#access-list access-list-number
  {permit | deny} protocol source source-wildcard [operator port]
  destination destination-wildcard [operator port] [established] [log]
```

- **Access-list-number** : Entry가 속할 list 번호 설정. **100~199, 2000~2699** 사이의 번호가 들어간다
- **permit | deny | remark** 는 해당 Entry에 매치되면 취할 Action을 정의
- **Source와 Destination** 은 송수신지 IP Address를 정의한다
- **mask** 는 Wildcard mask를 사용하여 Address 필드의 어느 비트들이 일치되어야 하는지 설정한다
- **Operator port** 는 lt(less then), gt(greater than), eq(equal to), neq(not equal to)와 Protocol Port번호를 명시한다
- **established** 는 Inbound TCP에 대해서만 사용된다
- **log** 는 Console로 log Message를 보낸다

```
Router(config-if)#ip access-group access-list-number {in | out}
```

## 14. NAT — 동적(Dynamic)

**① IP 변환에 사용할 전역 주소풀을 설정한다**

```
Router(config)#ip nat pool name start-ip end-ip {netmask Netmask | prefix-length Prefix-length}
```

**② 내부에서 IP변환을 허용할 주소를 Standard Access-list로 정의한다**

```
Router(config)#access-list number permit source-address [wildcard-mask]
```

**③ 동적 변환을 수립하기 위한 NAT 설정을 한다**

```
Router(config)#ip nat inside source list Access-list-number pool name [overload]
```

**④ 각 인터페이스로 이동 후 내부와 외부를 각각 설정한다**

```
Router(config-if)#ip nat inside
Router(config-if)#ip nat outside
```

`overload`를 붙이면 포트까지 써서 공인 주소 하나를 여러 사설 주소가 공유합니다(PAT).

## 15. NAT — 정적(Static)

내부 서버(`192.168.100.5/24`)를 공인 주소(`210.123.94.10`)에 고정으로 매핑하는 구성입니다.
내부 스위치 — 라우터 E0(내부) / S0(외부) — ISP 로 이어집니다.

**정적 변환을 수립하기 위한 NAT 설정을 한다**

```
Router(config)#ip nat inside source Static local-ip global-ip
```

**각 인터페이스로 이동 후 내부와 외부를 각각 설정한다**

```
Router(config-if)#ip nat inside
Router(config-if)#ip nat outside
```

## 16. VPN 종류

기업 네트워크에 안전하게 접속하는 방식은 크게 두 가지입니다.

1. **Remote Access VPN** — 개인이 회사망에 접속
2. **Site-to-site VPN** — 지사와 본사를 통째로 연결

## 17. 시스템 실습 범위

**Rocky Linux / Windows Server / Ubuntu Linux — 네트워크 환경 설정 후 다양한 서버 구축 실습**

클라이언트가 정보 및 서비스를 요구하면 서버가 제공하는 구조이며, 서버 쪽에
**웹 / DB / FTP / DNS / ETC** 를 올립니다. 리눅스 서버와 윈도우 서버를 함께 다룹니다.

리눅스 쪽 학습 순서는 설치 → 기본 명령어 → 사용자 및 그룹 정책 → 서버 관리 → 서버 구축입니다.

## 18. 리눅스 파일과 디렉터리 구조

| 경로 | 이름 | 설명 |
|---|---|---|
| `/` | 루트 | 최상위(루트) 디렉터리 (절대 경로를 표기할 때 이 디렉터리로부터 시작함) |
| `/bin` | 바이너리 | 이진파일(실행파일) / 기본적인 명령어가 저장된 디렉터리 (자주 사용하는 `mv`, `cp`, `rm` 등과 같은 명령어들이 여기에 존재함) |
| `/boot` | 부트 | 리눅스의 부트로더(Boot loader)가 있는 디렉터리 |
| `/dev` | 디브(디바이스) | 시스템 디바이스(device)파일을 저장하고 있는 디렉터리 (`/dev/sda`(하드디스크), `/dev/cdrom`(CD-ROM) 등과 같은 장치 파일들이 여기에 위치함) |
| `/etc` | | 설정파일들 두는 디렉터리 |
| `/home` | 홈 | 사용자의 홈디렉터리 (사용자를 추가하면 사용자의 id와 동일한 디렉터리가 자동으로 생성) |
| `/lib` | 립(라이브러리) | 커널이 필요로 하는 각종 라이브러리 파일, 커널 모듈파일 등이 존재하는 디렉터리 |
| `/media` | 미디어 | DVD, CD-ROM, USB 등의 탈부착 가능한 장치들의 마운트 포인트로 사용하는 디렉터리 |
| `/mnt` | 마운트 | 탈부착 가능한 장치들에 대한 마운트 포인트로 사용하는 디렉터리 |
| `/opt` | 옵트 | 응용프로그램 패키지 설치 장소로 패키지 매니저가 자체적으로 설치 및 삭제를 수행함 |
| `/proc` | 프록(프로세스) | '가상파일시스템'으로 현재 메모리에 존재하는 작업들이 파일 형태로 존재하는 곳 |
| `/root` | 루트 | 관리자계정 root 사용자의 홈디렉터리 |
| `/sbin` | 시스템 바이너리 | 시스템 이진파일(실행파일) (`ifconfig`, `ethtool`, `halt`, `e2fsck`와 같은 시스템 명령어들을 저장하고 있는 디렉터리) |
| `/usr` | 유저 | 일반 사용자들이 사용하는 디렉터리 |
| `/var` | 바 | 시스템 운영 중 생성되었다가 삭제되는 데이터를 임시 저장하기 위한 공간으로 사용되는 디렉터리 |

## 19. vi 편집기 명령 요약

**커서 이동**

| 키 | 동작 |
|---|---|
| `h` | 한 칸 왼쪽으로 이동 |
| `j` | 한 줄 아래로 이동 |
| `k` | 한 줄 위로 이동 |
| `l` | 한 칸 오른쪽으로 이동 |
| `0` | 현재 줄의 맨 앞으로 이동 |
| `$` | 현재 줄의 맨 끝으로 이동 |
| `Ctrl + F` | 한 화면 아래로 이동 |
| `Ctrl + B` | 한 화면 위로 이동 |
| `gg` | 파일의 처음으로 이동 |
| `G` | 파일의 끝으로 이동 |

**삽입 (입력 모드로 전환)**

| 키 | 동작 |
|---|---|
| `i` | 커서 위치 앞에서 삽입 |
| `a` | 커서 위치 뒤에서 삽입 |
| `I` | 현재 줄의 앞에 삽입 |
| `A` | 현재 줄의 끝에 삽입 |
| `o` | 현재 줄 아래에 한 줄 열기 |
| `O` | 현재 줄 위에 한 줄 열기 |
| `u` | 편집 버퍼를 수정했던 마지막 명령을 취소 |
| `U` | 편집 버퍼를 수정했던 마지막 명령 반복 |

**문자 삭제**

| 키 | 동작 |
|---|---|
| `x` | 커서가 있는 문자 삭제 |
| `X` | 커서의 왼쪽 문자 삭제 |
| `D` | 커서부터 줄의 끝까지 삭제 |
| `dd` | 현재 줄 전체 삭제 |
| `dG` | 커서부터 편집 버퍼의 끝까지 삭제 |
| `d1G` | 커서부터 편집 버퍼의 맨 앞까지 삭제 |

**문자 복사·탐색**

| 키 | 동작 |
|---|---|
| `yy` | 한 줄 복사 |
| `5yy` | 현재 커서부터 5줄 복사 |
| `p` | 현재 라인 밑에서부터 붙여넣기 |
| `/` | 전방 탐색 |
| `?` | 후방 탐색 |

## 20. YUM 도구 명령어

| 명령 | 설명 |
|---|---|
| `yum list` | 설치된 패키지와 업데이트 가능한 패키지 보기 |
| `yum list installed [패키지명]` | 설치된 패키지 리스트 확인 (특정 패키지 설치 여부 확인) |
| `yum list update` | 설치된 패키지 중 업데이트된 패키지 목록 확인 |
| `yum list available` | 설치 가능한 패키지 목록 확인 |
| `yum check-update` | 업데이트 가능한 패키지 패키지 보기 |
| `yum install -y [패키지명]` | 패키지 설치 |
| `yum update -y` | 패키지 업데이트 |
| `yum remove [패키지명]` | 패키지 제거 |
| `yum search [패키지명]` | 패키지 검색 |
| `yum info [패키지명]` | 패키지 정보 알아보기 |
| `yum grouplist` | 그룹별 그룹별 패키지 설치 정보 확인 |
| `yum -y groupinstall "Development Tools"` | 이 그룹에 속한 모든 패키지 설치 |
| `yum clean all` | yum 관련해서 저장된 정보 삭제 |

## 21. 다양한 서버 구축 및 설정 (실습 대상)

- **WEB-DB** (HTTP/HTTPs)
- **FTP** (SFTP/FTPs)
- **DNS**
- **NFS**
- **Samba**

## 22. DNS ZONE 파일 설정

| 레코드 | 설명 |
|---|---|
| **SOA**(Start Of Authority) | 해당 도메인에 대하여 설정된 네임서버가 모든 정보를 가지고 있음을 선언 |
| Serial | 보통 날짜 형식(YYYYMMDD) - 2차 네임 서버와의 연동에 관련된 값 |
| Refresh | 2차 네임 서버가 1차 네임 서버로부터 데이터를 가져온 후 다시 업데이트하기 위해 기다리는 시간 |
| Retry | 2차 네임 서버가 1차 네임 서버로 접속을 실패하였을 경우 지정된 시간이 지난 후 다시 재접속하는 시간 |
| Expire | 1차 네임 서버에서 받아온 도메인 정보를 보관하는 주기로 지정된 시간이 지나면 받아온 데이터는 신뢰성이 없다고 판단하고 삭제 |
| Minimum | TTL(Time To Live) - 도메인 정보를 얼마나 보관할 지에 대한 시간 |
| **NS** | 해당 도메인의 네임서버 지정 |
| **A** | 해당 도메인의 실제 IP주소를 설정 |
| **MX**(Mail eXchanger) | 해당 도메인의 메일서버 지정 |
| **CNAME** | "Canonical NAME" - 특정 도메인에 대한 서브 도메인을 사용하기 위한 레코드 |
| **PTR** | IP주소에 대해 도메인명을 매핑 - Reverse Zone파일에서 사용 |
| **AAAA** | IPv6주소에서 사용되는 A레코드 |

`$TTL` – SOA 레코드의 Minimum과 같은 의미 (Minimum 값보다 우선)

## 23. Syslog Facility

Facility는 로그 메시지의 출처를 나타내는 분류 체계이며, Priority Level은 로그 메시지의
중요도(긴급성)를 분류하는 체계입니다.

| Facility | 설명 |
|---|---|
| `auth` | 인증 관련 로그 |
| `authpriv` | 민감한 인증 정보 |
| `cron` | cron 데몬의 스케줄링 로그 |
| `daemon` | 일반 데몬 프로세스 |
| `kern` | 커널 메시지 |
| `lpr` | 프린터 관련 로그 |
| `mail` | 이메일 관련 로그 |
| `news` | 뉴스 데몬 관련 로그 |
| `syslog` | syslog 데몬 자체의 로그 |
| `user` | 일반 사용자 프로그램의 로그 |
| `uucp` | Unix to Unix 복사 프로토콜 관련 로그 |
| `local0 ~ local7` | 사용자 정의 facility (Custom Facility) |

## 24. Syslog Priority Level

`emerg` = 0 … `debug` = 7 로 번호가 매겨집니다.

| Priority Level | 심각도 | 설명 |
|---|---|---|
| `emerg` | 긴급 | 시스템이 사용 불가능한 상태 (즉각조치 필요) |
| `alert` | 경고 | 즉각적인 조치가 필요한 상황 |
| `crit` | 치명적 | 심각한 상황이지만 시스템 전체에는 영향이 없는 경우 |
| `err` | 오류 | 일반적인 오류 발생 (파일 접근 실패) |
| `warning` | 경고 | 위험은 아니지만 주의가 필요한 상태 |
| `notice` | 주목 | 정상 동작 중이지만 중요한 이벤트 (구성 변경) |
| `info` | 정보 | 정보 제공용 메시지 (서비스 시작/종료) |
| `debug` | 디버그 | 문제 해결을 위해 추가 정보 제공 (내부 상태, 변수 값 출력) |

이어서 **rsyslog / loganalyzer 구축 실습**과 **Windows Server(web, ftp, dns 및 기본 설정)** 으로 넘어갑니다.

## 25. 클라우드 서비스 모델

**(Infra / Platform / Software) as a Service — AIaaS**

클라우드 서비스 별 관리 범위입니다. 아래에서 위로 네트워킹 → 스토리지 → 서버 → 가상화 →
OS → 미들웨어 → 런타임 → 데이터 → 애플리케이션 순으로 쌓입니다.

| 모델 | 본인이 관리 | 공급 업체가 제어 및 관리 |
|---|---|---|
| **IaaS** | 애플리케이션, 데이터, 런타임, 미들웨어, OS | 가상화, 서버, 스토리지, 네트워킹 |
| **PaaS** | 애플리케이션, 데이터 | 런타임, 미들웨어, OS, 가상화, 서버, 스토리지, 네트워킹 |
| **SaaS** | 없음 | 전 계층 |

## 26. 퍼블릭 클라우드 vs 프라이빗 클라우드

| | 퍼블릭 클라우드 | 프라이빗 클라우드 |
|---|---|---|
| **장점** | 유지 및 관리 용이<br>시스템 확장 용이<br>사용량에 따른 지불<br>비용 절감 | 높은 보안성<br>넓은 네트워크 대역폭<br>개별 애플리케이션의 개발 가능<br>커스터마이징 유연성 |
| **단점** | 이용자 관리 권한 낮음<br>데이터 보안 100% 보장 불가<br>표준화 된 애플리케이션만 사용 가능 | 낮은 탄력성<br>IT 인력 구축 필요<br>장비 구축에 고비용 발생<br>데이터 센터 구축 비용 발생 |

둘을 함께 쓰는 것이 **Hybrid Cloud**입니다.

## 27. AWS 주요 서비스

**Amazon Cloud Compute**

| 서비스 | 설명 |
|---|---|
| Amazon EC2 | 아마존 클라우드 상의 유연하며 가상 서버 (Elastic Virtual servers in the cloud) |
| Auto Scaling | EC2 용량 및 Instance 확장에 대응한 자동 스케쥴링 기능 (Automated scaling of EC2 capacity) |
| Elastic Load Balancing | 동적 트래픽 분배 기능 (Dynamic traffic distribution) |

**Amazon Storage, Database**

| 서비스 | 설명 |
|---|---|
| Amazon EBS | EC2와 네트워크로 연결된 블록 스토리지 (Block storage for use with Amazon EC2) |
| Amazon S3 | API를 통한 인터넷 규모의 오브젝트 스토리지 (Internet scale object storage via API) |
| Amazon RDS | 관계형 데이터베이스 관리 서비스 (Managed relational database service) |

**Amazon CDN & Management**

| 서비스 | 설명 |
|---|---|
| Amazon CloudFront | 세계로의 콘텐츠 배포 (Distribute content globally) |
| Amazon CloudWatch | 클라우드 자원의 모니터링 (Monitor resources) |
| AWS IAM (Identity & Access Mgmt) | 사용자, 그룹 및 권한 관리 (users, groups & permissions) |

**Amazon Network**

| 서비스 | 설명 |
|---|---|
| Amazon Virtual Private Cloud(VPC) | 사설 격리형 AWS 클라우드 (Private, isolated section of the AWS Cloud) |
| Amazon Route 53 | DNS(Domain Name System) 웹서비스 (Domain Name System (DNS) web service) |
